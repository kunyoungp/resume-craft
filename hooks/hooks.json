{
  "description": "resume-craft plugin hooks",
  "hooks": {
    "UserPromptSubmit": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -f \".resume-craft/profile.json\" ]; then echo '{\"suppressOutput\":false,\"systemMessage\":\"resume-craft: User has a profile at .resume-craft/profile.json. Commands: /resume [url], /resume-profile, /resume-templates\"}'; else echo '{\"suppressOutput\":true}'; fi",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
